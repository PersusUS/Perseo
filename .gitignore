# =========================
# General / OS
# =========================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# =========================
# Environment Variables / Secrets
# =========================
.env
.env.*
!.env.example
*.pem
*.key
*.cert
*.pfx
*.jks
*.keystore
tokens.json
credentials.json
client_secret.json
service-account.json

# =========================
# Node / Frontend / Vite
# =========================
node_modules/
dist/
dist-ssr/
build/
.npm/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache
.vite/

# =========================
# Rust / Tauri
# =========================
/RealTime/src-tauri/target/
/RealTime/src-tauri/gen/schemas/
target/
**/*.rs.bk

# =========================
# Python / RAG
# =========================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.eggs/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# NOTA: aquí NO van las reglas genéricas de empaquetado de Python
# (lib/, lib64/, build/, dist/, parts/, var/, downloads/, sdist/, wheels/).
# Git las aplica a CUALQUIER carpeta con ese nombre en todo el árbol, y `lib/`
# se estaba tragando RealTime/src/lib/ — el núcleo de la aplicación. Ver H-01.
# Este proyecto no genera distribuciones de Python, así que no hacen falta.

# =========================
# Datos personales / privados
# =========================
# El vault vive fuera del repositorio desde el 2026-08-24 (en `Documents/Persus`,
# carpeta `10_PERSEO/`), así que esta regla ya no tapa nada. Se queda como red:
# contiene nombres reales y descripciones de personas identificables, y el día
# que alguien vuelva a dejar un vault aquí dentro no debe salir del disco (H-04).
obsidian_vault/

# =========================
# Restos del RAG de v1
# =========================
# La base vectorial se retiro con el paso a v2: la memoria la lleva el agente
# `memoria` del nucleo, sobre el mismo vault y sin embeddings. La regla se queda
# por si queda alguna carpeta en discos que vienen de antes. Ver H-03.
RAG/

# =========================
# Grafo de conocimiento regenerable
# =========================
# Salida de /graphify: se reconstruye leyendo el repositorio y pesa ~1,1 MB en
# dos ficheros (graph.html y graph.json). Además retrata la estructura del
# proyecto en un momento concreto, así que envejece mal: lo que valga la pena
# recordar del grafo va en la bitácora, no aquí.
graphify-out/

# =========================
# Estado del núcleo (perseo-core)
# =========================
# Cola de trabajos y token de acceso. La cola guarda el texto literal de lo que
# le pides a Perseo, y el token es la credencial que abre la API. Ninguno de los
# dos debe salir del disco local.
perseo_core/datos/

# Environments
.env/
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# =========================
# IDEs
# =========================
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# =========================
# Media / Custom Assets
# =========================
*.mp3
*.wav
*.mp4
*.mkv

# Senal efimera de auto-llamada que deja el detector de aplausos (H-09)
.perseo-autollamada

# La otra senal: `perseo` pide que la ventana vuelva de la bandeja, sin llamada.
.perseo-mostrar

# PID que deja la app para que el detector sepa si esta abierta (presencia.rs).
# Estado de ejecucion en la raiz del repo: mismo caso que H-09.
.perseo-app.pid

# Configuracion local de Codex CLI: herramienta del equipo, no del proyecto.
.codex/

# Restos de sesion del servidor Playwright MCP (consolas y capturas de pagina).
.playwright-mcp/
