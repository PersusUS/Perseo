[pytest]
testpaths = pruebas
python_files = test_*.py
addopts = -q --tb=short
# Los verificadores de perseo_core/ no son pruebas de pytest: levantan el nucleo
# de verdad y tardan minutos. Se ejecutan a mano, y estan listados en el README.
